# ⚠️  ARCHIVO DE EJEMPLO — Los valores aquí son PLACEHOLDERS falsos.
# NO usar estos secretos en producción.
# Copia este archivo a .env y reemplaza con valores reales generados con:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
#
# Los secretos reales están en .env (git-ignored) y NUNCA deben commitearse.

SESSION_SECRET=CHANGE_ME_USE_CRYPTO_RANDOM_BYTES_48

PORT_SERVER=8000

DB_PORT=3306
DB_HOST=localhost
DB_NAME=db_arquitectura
DB_USER=root
DB_PASS=CHANGE_ME_DB_PASSWORD

TOKEN_KEY=CHANGE_ME_USE_CRYPTO_RANDOM_BYTES_48

MONGODB_CNN=mongodb://localhost:27017/prodesign

SECRET_JWT_SEED=CHANGE_ME_USE_CRYPTO_RANDOM_BYTES_48